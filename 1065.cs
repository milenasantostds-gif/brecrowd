
using namespace std;

int main() {
    int valor, pares = 0;

    for (int i = 0; i < 5; i++) {
        cin >> valor;

        if (valor % 2 == 0) {
            pares++;
       Console.Writelilner("valores pares")

    return 0;
}
    }
}
